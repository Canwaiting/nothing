package dao;

public class UseDao implements Dao {
    @Override
    public boolean login(User user) throws Exception {
        if(user.name.equals()){
            return true;
        }
        return false;
    }
}
